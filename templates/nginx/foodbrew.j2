server {
        server_name foodbrew.{{ server_name }};

        root /var/www/foodbrew/www;

        index index.html index.htm index.nginx-debian.html;

        location / {
                try_files $uri $uri/ =404;
        }

        # Pass off the handler to Apache.
        # location /handler {
        #        proxy_pass http://127.0.0.1:8888;
        # }
}
